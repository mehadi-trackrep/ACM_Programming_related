#include <stdio.h>
#include <string.h>
int main()
{
	char name[50][32],giver[50][32],
	int n1,n2,i,taka;
	while(scanf("%d",&n1) == 1){
	    
	    for(i = 0;i < n1;i++)
	        scanf("%s",&name[i]);
	        
	       for(i = 0;i < n1;i++){
	        
	        scanf("%s",&name[i]);
	       }
	       
	    
	}
	
	return 0;
}