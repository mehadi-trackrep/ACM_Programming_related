#include <stdio.h>
#include <string.h>
int main()
{
	char str[50];
	gets(str);
	int len,i,j,count = 0;
	len = strlen(str);
	for(i=0;i<len;i++){
		for(j=0;j<len;j++){
			if(str[i]==str[j])
		}
	}
		
	
	return 0;
}