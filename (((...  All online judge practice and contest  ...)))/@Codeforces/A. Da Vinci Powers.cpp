#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long n,val;

    while(cin >> n)
    {

        val = (long long)pow(2,double(n));

        printf("%I64d\n",val);

    }

    return 0;
}
