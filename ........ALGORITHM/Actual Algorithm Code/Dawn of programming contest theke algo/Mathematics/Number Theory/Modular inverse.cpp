#include <bits/stdc++.h>
using namespace std;

 /// If M is prime then, b^(−1) ≡ b^(M−2) mod M . r na hole --> b^(−1) ≡  b^(ϕ(M)−1) mod M ; but shekhettere  M and  b must be co-prime.

int main()
{

    return 0;
}
