#include <stdio.h>
int main()
{
    char str[100] = "''mehadi hasan''";
   /// str [100] = "mehadihasan"; /// not possible ///
   /// gets(str);
   // puts(str);
   printf("%s",str);

    return 0;
}
