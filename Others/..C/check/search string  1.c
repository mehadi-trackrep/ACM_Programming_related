#include <stdio.h>
#include <string.h>
int main()
{
    char str1[1000],str2[100];

    len1 = strlen(str1);
    len2 = strlen(str2);

    for(i = 0 ; i < len1 ;i++){
        for(j = 0 ; j < len2 ; j++){
            if(str2[k] == str1[j])
        }
    }

    return 0;
}
