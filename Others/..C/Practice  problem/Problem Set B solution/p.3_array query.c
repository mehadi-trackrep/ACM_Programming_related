#include <stdio.h>
///#include <string.h>
int main()
{
    int n,i;
    scanf("%d",&n);
   // getchar();
    char a[5];
    gets(a); puts(a);  ///printf("%s",a);
  ///  for(i=0;i<n;i++)

    return 0;
}
