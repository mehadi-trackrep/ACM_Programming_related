#include <stdio.h>
#include <string.h>
int main()
{
    char a[] = "madam",b[] = "madam"; /// palindrome check
   /// gets(a);
   /// gets(b);

    if (strcmp(a,b) == 0)
        printf("OK");
    else ;

    return 0;
}
